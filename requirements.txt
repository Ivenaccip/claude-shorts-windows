# Pinned to the versions verified working on Windows 10 + CUDA (2026-07).
# torch/torchvision/torchaudio are installed separately by setup.ps1
# (index-url depends on GPU availability).
faster-whisper==1.2.1
mediapipe==0.10.35
numpy==2.5.1
opencv-python==5.0.0.93
